/**
 * map country names to new arr
 */
const countryNames = countries.map(country => country.name)
console.log(countryNames)


/**
 * map can return anything
 */
 const gibberish = countries.map(country => 'Coca Cola')
 console.log(gibberish)